# Version control in Jupyter notebooks

## Can you just use git?

Jupyter notebooks are just files, so the default option is to version control

## Use a Jupyter-compatible notebook with version control
